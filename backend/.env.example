# Example environment — copy to `.env` and fill in.
# Google Maps (Places API): ใช้โดย `/places/nearby` และ `/places/photo` ให้แอปเรียกผ่าน backend แทนการยิงคีย์จากมือถือตรงไปที่ Google (กรณีคีย์จำกัดแอนดรอยด์มักได้ REQUEST_DENIED)
GOOGLE_MAPS_API_KEY=
