# Environment / secrets
.env
*.env.local
*.env.secret
docker/.env
docker/.env.secret
.wrangler/
**/.wrangler/

# Cloudflare Tunnel credentials — NEVER commit (gives full access to the tunnel)
.cloudflared/
*.pem

# Flutter secrets (contains real API keys — never commit)
flutter_application_1/lib/config/secrets.dart

# Python virtual environments — must NEVER be committed
backend/venv/
backend/.venv/
venv/
.venv/

# Python bytecode
__pycache__/
**/__pycache__/
*.pyc
*.pyo
*.pyd

# Python tooling
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# Backend debug / scratch dumps
backend/current_db.txt
backend/db_output.txt
backend/db_output_utf8.txt
backend/output_mermaid.txt
backend/.runtime/
backend/logs/

# IDE
.idea/
.vscode/

# Claude local settings (may contain personal paths / credentials)
.claude/settings.local.json
.claude/worktrees/

# Android Gradle build cache
flutter_application_1/android/.gradle/
flutter_application_1/android/app/build/
flutter_application_1/android/.kotlin/
flutter_application_1/android/app/src/main/kotlin/com/example/

# Flutter generated / lock files (optional — remove if your team checks these in)
flutter_application_1/.flutter-plugins-dependencies

# Release keystore — NEVER commit
flutter_application_1/android/key.properties
flutter_application_1/android/app/*.jks
flutter_application_1/android/app/*.keystore

# Third-party infra clones (cloned externally, not part of this repo)
# docker/ is a clone of https://github.com/kesor/ollama-proxy
docker/

# Scratch/debug scripts not meant for production
backend/check_all_public_tables.py
backend/check_public_tables.py
backend/status_admin.py
backend/verify_out.txt
backend/verify_system.py
backend/test_ai_prompt
project_structure.txt

# Local HTML/architecture drafts (root-level only, not kotlin package)
/CaloriesGuard/

# LoRA fine-tune artifacts (large; share via git-LFS or release artifacts)
notebooks/calories_guard_adapter/
notebooks/calories_guard_adapter_best/
notebooks/_run_outputs/
notebooks/_run_full.log
notebooks/_exec.log
notebooks/out/

# Editor / IDE local config
.cursor/
.claude/scheduled_tasks.lock
~$*

# Build artifacts and one-off scratch
flutter_application_1/build_error.txt
_schema_inventory.md
backup_before_v22_v24.dump
